# GenAI Recruiter Bot — Claude Code Context

## What this is
Multi-agent SMS-style recruiting chatbot (Main Agent + Exit/Sched/Info advisors)
for a Python Developer role. Full spec: **docs/PROJECT_SPECIFICATION.md** — READ IT
before any non-trivial change. The turn behavior contract is **docs/one_turn_flowchart.json**
(implemented as a LangGraph state graph, spec §4). Task plan & IDs: **docs/PROJECT_TASKS.md**.

## Commands
- Run terminal chat:   `python -m app.main`   (config check: `python -m app.main --check-config`)
- Build vector index:  `python -m app.modules.embedding.build_index`   (Phase 1)
- Build SQLite DB:     `python -m app.modules.scheduling.db_setup`     (Phase 1)
- UI:                  `streamlit run streamlit_app/streamlit_main.py` (Phase 6)
- Tests:               `pytest` (zero real API calls)   Real-API scenarios: `pytest -m real_api`   Lint: `ruff check . && ruff format .`

## Hard rules
- Never commit `.env` or API keys. All constants live in `app/config.py` (from .env). Zero magic numbers in logic.
- Every LLM decision goes through a Pydantic schema (structured output; no free-text parsing). Parse failure → retry once → deterministic fallback.
- Advisors NEVER emit user-facing text; only the Main Agent talks to the user (rule R-4).
- Every advisor receives the COMPLETE chat history, never a summary (rule R-2).
- SQL is queried only when Sched Advisor rules `sched`; Chroma only when Info Advisor rules `info_needed` (rule R-3).
- Scheduling slots must be verified `available = 1` in the DB in the SAME turn they are offered (S-3, hard constraint).
- Max 3 advisor consultations per turn (guard R-1); guard trip → default `continue` + clarifying question.
- Every emitted turn carries exactly one action label: `continue` | `schedule` | `end` (rule R-5).
- "Now" for date resolution comes from `settings.now()` — never `datetime.now()` directly (demo override, risk #4).
- New logic ⇒ new/updated pytest tests in the same commit. Keep `ruff` clean.

## Workflow
Explore → Plan (show me first) → Implement → Test → Commit (conventional commits: feat/test/docs/fix).
Work task-by-task per docs/PROJECT_TASKS.md (GRB-xxx IDs); one task = one branch = one PR.
Do not skip acceptance criteria. Append a line to docs/DEVLOG.md at the end of each session.

## Current status
*(Last verified 2026-07-19 — see docs/PROJECT_TASKS.md §0 for the live per-GRB-task status table, and chat/DEVLOG for detail; reconcile after every epic.)*
- ✅ Epic E0 (bootstrap) — complete.
- ✅ GRB-010 (SQLite port) and GRB-011 (ScheduleRepository) — done, tested (`tests/test_scheduling_repository.py`).
- ✅ GRB-012 (embedding pipeline) — done, tested. Fixed a real bug: `data/raw/Python_Developer_Job_Description.pdf` is actually a zip bundle (manifest.json + per-page .txt/.jpeg), not a real PDF; `build_index.py` now detects and reads that format directly instead of decoding the raw zip bytes as UTF-8 (was producing garbled/unreadable answers). Still uses a deterministic hash-embedding fallback when no OpenAI key/chromadb is present, not real OpenAI embeddings — semantic retrieval quality depends on a real `OPENAI_API_KEY` in `.env`.
- ✅ GRB-020 (AdvisorOutput Pydantic contracts) — `app/schemas.py` (`ExitAdvisorOutput`, `SchedAdvisorOutput`, `InfoAdvisorOutput`, `MainAgentOutput`) + `app/structured_output.py::get_structured_output` (retry-once-then-fallback, rule N-3), unit tested.
- ✅ Turn graph consolidated to the spec-mandated location: `app/graph.py` + `app/state.py` are the real implementation; the duplicate `app/modules/main_agent/{graph,state}.py` were deleted. `app/main.py` and tests updated accordingly (`tests/test_graph.py`).
- ✅ GRB-021 (Info Advisor), GRB-023 (Sched Advisor incl. `date_resolver`/`tools.py`), GRB-024 (Exit Advisor) — all three now make real OpenAI structured-output calls (`app/llm_client.py` shared client) via `chat.completions.parse`, wrapped in `get_structured_output` for retry/fallback. `app/graph.py` now honors each advisor's actual decision instead of assuming the outcome implied by keyword-routing (e.g. routing to "exit" no longer force-ends the conversation — only an actual `end` decision does). Manually smoke-tested end-to-end against the real API for all three paths (info/sched/exit) plus the "routed-to-exit-but-really-a-reschedule" case.
  - `date_resolver.resolve_relative_date` is deliberately **not** LLM-assisted — pure `dateutil`, for determinism/testability on the four spec-required expressions (tomorrow/next Friday/Monday at 3pm/in two weeks) plus month-name expressions ("April 2024"). Returns `DateRange | None` — `None` on an unrecognized expression, **never** a silent guess (it used to default to "tomorrow"; fixed after manual testing surfaced a wrong-guess-with-no-signal bug).
  - Sched tool invocation (`tools.py`, LangChain `@tool`s wrapping `ScheduleRepository`) is code-driven, not autonomous LLM function-calling — our code calls the tool once the advisor decides `"sched"`, guaranteeing S-3 (slots always DB-verified in-turn) regardless of what the model itself proposes.
  - Fine-tuning (Epic E3, GRB-030-033) is implemented and tested (see the Epic E3 bullet below) — `exit_advisor` uses the prompted path today (`settings.exit_advisor_finetuned_model` is honored if ever set, but empty, since a real fine-tuned model can't currently be produced on this account).
- ✅ GRB-025 (advisor tests) — `tests/test_exit_advisor.py`, `tests/test_sched_advisor.py`, `tests/test_date_resolver.py`, `tests/test_info_advisor.py`, `tests/test_main_agent.py`, plus `tests/test_graph.py` — all mock each advisor's/router's `_call_llm`/`decide`/`draft_answer`/`route` entry point, so the default `pytest` run makes zero real API calls.
- ✅ Epic E4 (Main Agent orchestration) — **closed, lightweight version** (not the full spec-literal version — see "Not done" below). `app/modules/main_agent/{agent,prompts}.py` hold a real LLM routing decision (`RoutingDecision` schema: exit/sched/info/respond) that replaced the old keyword-based `_route_to_advisor`, and `app/graph.py::run_turn` is a bounded loop (guard R-1, max 3 per turn) that can consult more than one advisor per turn before synthesizing the final `MainAgentOutput`. GRB-043: `run_turn` returns a per-advisor `trace` (advisor/decision/reason); `main.py` prints it. GRB-044: `tests/verify_canonical_flows.py` scripts the 4 canonical flows (Q&A, scheduling incl. relative date, refusal→end, opt-out→end) against the live graph and API — all 4 pass (`python -m tests.verify_canonical_flows`).
  - **Not done**: this still isn't the `langgraph`-library, node-for-node flowchart implementation the spec's tech stack literally mandates (§3.3) — it's equivalent behavior in plain Python control flow. Adopting the actual `langgraph` `StateGraph` API is a deliberately deferred, separate, bigger rewrite (discussed and explicitly declined twice now — most recently 2026-07-19 — in favor of the lighter option).
  - `main_agent.route()` takes a phase hint (`"schedule"` for as long as an offer is still open — see `app.graph._in_schedule_phase` — else the prior turn's literal action) so routing/synthesis can favor Sched for ambiguous date-ish replies anywhere in an open scheduling thread, not just the single turn right after the offer.
- 🟡 Epic E5 (evaluation) — GRB-050/051/052 done, GRB-053 (tune & re-run) done as an honest-gap-analysis outcome (S-1's 85% not met), GRB-054 (stretch ablations) not started. `tests/test_evals.ipynb` replays all 44 evaluable labeled turns (via `tests/eval_replay.py`'s case-building logic) through the real graph/API, computes accuracy + per-class P/R/F1 + a 3×3 confusion-matrix heatmap (saved to `docs/eval_confusion_matrix.png`), and lists every miss with its advisor trace. Two tuning passes: baseline 31.8% (14/44) → iteration 1 (Sched Advisor: qualifying-info/declined-time → `sched`; Main Agent: affirmations-after-an-offer → `sched`) regressed to 29.5% (13/44) — the classifier fix was correct but exposed that `decide()` unconditionally tried to parse a date out of messages that had none → iteration 2 (`decide()` now defaults to the nearest available slots when no date is named at all, vs. declining to ask when a date was genuinely attempted but garbled) reached **52.3% (23/44)**. That iteration 2 change deliberately updated one existing test (`whenever works for you` now defaults instead of declining) — a reasoned behavior change, not a silent regression of the original anti-guessing protection. Full before/after numbers and the 3 remaining named failure patterns (ranked by impact) are in the notebook's error-analysis section — the two highest-impact ones need a design decision (a richer routing signal for continue-vs-schedule ambiguity; sequential full-conversation replay instead of per-turn-isolated replay for confirmed-booking cases) rather than another prompt patch.
- ✅ Epic E6 (Streamlit UI) — registration form → chat UI → dev trace panel, all wired to the real `app.graph.run_turn` (zero decision logic in the UI layer). `st.secrets` bridged to env vars for Community Cloud, falling back cleanly to `.env` locally (fixed a real bug: bare `_key in st.secrets` raises `StreamlitSecretNotFoundError` when no `secrets.toml` exists at all, not just on a missing key — wrapped in try/except). GRB-063 (actual Streamlit Community Cloud connection) needs the user's own account/GitHub push, so it's the one sub-item not done from here. Verified via real `streamlit run` (clean boot, zero exceptions) plus a full interactive pass using Streamlit's own `AppTest` framework (no `chromium-cli`/Playwright/Node available in this environment) — registration submit, a real chat turn with a real API call (correctly showed live DB slots), dev-mode trace panel, and Reset all confirmed working end-to-end.
- ✅ Epic E3 (Exit Advisor fine-tuning, GRB-030..033) — **closed**, all four items implemented and tested (`app/modules/fine_tuning/{dataset_builder,launch_job,compare}.py`, `tests/test_fine_tuning_dataset.py`, `tests/test_launch_job.py`, `tests/test_compare.py`). Dataset builder trains on the real production message shape (`history_to_messages` + the real `SYSTEM_PROMPT`, structured-output-JSON assistant targets) instead of the task spec's literal bare-token pseudocode, for train/serve parity — see `docs/DEVLOG.md`'s "cont. 8" entry for the full rationale. Real run: 47 train (34 dataset + 13 hand-written GRB-031 edge cases) / 10 val, conversation-level split, zero leakage. **Real job launch attempted and blocked**: `fine_tuning.jobs.create` returned `403 training_not_available` — OpenAI is winding down the self-serve fine-tuning platform account-wide; this is not a code, cost, or quota issue, and no fine-tuned model can be produced on this account under current OpenAI policy. `compare.py` ran the prompted-only baseline for real on the val split (end: P=0.50 R=0.33 F1=0.40, n=10) and will score a fine-tuned row automatically if one ever becomes available. `EXIT_ADVISOR_FINETUNED_MODEL` stays empty; prompted remains the default — confirmed by the user in advance as an acceptable outcome, not a gap to chase.
- ✅ Epic E7 (docs/delivery) — GRB-070 (README) done: purpose, Mermaid architecture diagram, structure, setup, usage, eval results (both isolated and sequential numbers), honest "not yet deployed" note. GRB-071 (final quality pass) mostly done: `ruff check .` and `ruff format .` both clean repo-wide (19 files reformatted, all mechanical — `pytest` reconfirmed passing after), stray untracked `tmp_verify_db.py` removed, dead-code spot-check clean; demo screenshots/GIF not done (needs a real browser, unavailable here). GRB-072: **discovered the entire session's work (all of E1-E7) had been sitting uncommitted the whole time** — only 3 commits existed, the last from the E0 bootstrap phase — so tagging `v1.0` first required committing everything as one squashed commit (`47da09b`, user's explicit choice over several logical commits), reviewed for secrets/`.env` leakage before committing (clean), then tagged `v1.0` on top. Presentation outline explicitly skipped, not needed per user.
- 🟡 **CORE-REV (in progress, deferred for now by explicit user instruction — resume later)** — booking-completion path + proactive escalation + slot rendering, target ≥75% eval accuracy. FIX 2 (meaningless single-char input, e.g. "f") fixed via `app/graph.py::_looks_meaningless`. FIX 1 (ordinal/partial slot confirmation, e.g. "the second one") could **not** be reproduced despite 3 independent real-execution test paths — awaiting the user's live-reproduced transcript via the dev trace panel.
  - **Eval harness rebuilt** (`tests/eval_replay.py`): defaults to **sequential replay** (one real `ConversationState` per conversation, walked turn-by-turn through the real graph — matches spec §9's "feed the system the history up to that point" literally); `--mode isolated` kept for comparison. Also now tags every miss **GENUINE vs. DIVERGENCE** (a documented heuristic, not ground truth — see `_is_divergence_artifact`'s docstring) and reports raw + adjusted accuracy.
  - **Current: raw 52.3% (23/44), adjusted 67.6% (23/34)** excluding 10 divergence-artifact misses (2026-07-20 re-measurement). A real, confirmed win: `_looks_like_a_date_attempt`'s bare `\d` check wrongly flagged "3 years' experience"/"5 years of experience" as garbled date attempts — narrowed to date-shaped digit patterns only.
  - **2026-07-20**: 3 narrow, genuine (non-divergence) bugs fixed and each independently verified via a real-API regression test — Sched Advisor over-confirming vague enthusiasm as a booking (`app/modules/sched_advisor/advisor.py`'s `CONFIRMATION_PROMPT_ADDENDUM`), confirmation-matching failing on weekday-name references because the model had to compute day-of-week itself (fixed by annotating offered slots with their weekday name, computed in Python), and Exit Advisor over-firing on a soft/deferring decline (`app/modules/exit_advisor/prompts.py`). **The aggregate eval score did not improve** (moved from 54.5%/70.6% pre-fix to 52.3%/67.6%) — a pre-existing, unrelated `app/graph.py` action-labeling gap (`dont_sched`-after-offer restates slots under `action="continue"`, not `"schedule"`) was newly exposed by fix #1, and sequential mode's cascading nature (fixing one turn reshapes the rest of that conversation's real trajectory) surfaced 2 new misses elsewhere. Decision: kept the fixes (each is a real, tested correctness improvement) and deliberately stopped chasing the aggregate number further this session — full reasoning in `docs/DEVLOG.md`'s 2026-07-20 entry.
  - **Investigated and reverted**: the "over-eager escalation" pattern (proactive scheduling firing on ANY first experience-mention). Reading the actual dataset conversations side-by-side found conversations 1 and 4 have the *word-for-word identical* candidate first reply but opposite gold labels for the next turn — the ground truth is genuinely inconsistent for identical input across conversations, so no message-content-only signal can fully resolve it. A same-turn timing bug WAS real and fixed (`state.qualifying_info_shared` was armed and consumed within the same turn's loop; now deferred to after the loop — kept). A deterministic guard built on top of that (block same-turn re-consult escalation) was tried but caused a full-dataset regression (schedule recall 78.9%→31.6%) because the 5-conversation sample used to diagnose it wasn't representative of the dataset's actual majority pattern — **reverted**, kept as an `xfail`-marked regression test in `tests/test_scenarios.py` documenting why, not deleted. Revisit with a richer signal later, not another blanket rule.
  - **New convention**: `tests/test_scenarios.py` holds real-API pytest cases marked `@pytest.mark.real_api` (registered in `pyproject.toml`; excluded from the default bare `pytest` via `addopts = "-q -m 'not real_api'"`, run explicitly via `pytest -m real_api`) — **prefer this over an ad-hoc `python -c` snippet whenever a check can be expressed as a test**; ad-hoc snippets don't survive as regression coverage.
- A local `.env` exists (gitignored, real API key present) with `DEMO_NOW_OVERRIDE=2024-04-15T10:00:00Z` per `.env.example`'s own documented risk-#4 mitigation — required for the sched date-window logic to find slots in the 2024-seeded DB. **Do not ever put a real key in `.env.example`** — that file is git-tracked; only `.env` is gitignored.
- Stray file `tmp_verify_db.py` at repo root (untracked, ad-hoc DB-inspection script) should be removed or moved into a scripts/ dir — not part of any GRB task.
- `pytest`: 122/122 passing, zero real API calls in the default suite (real-API scenario tests live in `tests/test_scenarios.py`, marked `real_api`, run via `pytest -m real_api`). `ruff check .`: clean.
- **2026-07-19, fixed: rejected scheduling slots got re-offered (infinite loop)** — reported live transcript: offer Apr slots → candidate names a new date (15/5/24, correctly offered) → candidate rejects with "none" → bot fell back to re-offering the ORIGINAL April slots → "other dates" → same loop. Two real bugs: (1) rejection phrasings ("none", "other dates") were routed to the Exit Advisor (ending the conversation) instead of Sched — fixed in `main_agent/prompts.py`; (2) `date_resolver.default_forward_window` always anchored at `now` with no memory of conversation progress, and the Sched Advisor's confirmation prompt also misclassified bare rejections as vague `dont_sched` — fixed via `ConversationState.offered_slots_history` (every slot ever offered, not just the current batch), `default_forward_window(now, after=...)`, and `decide(..., previously_offered_slots=...)` (excludes ids, advances the floor date, reports honestly when nothing's left). Reproduced via a real-API test before touching code; 3 directive scenario tests added (reject→later slots; "other dates"→different slots; double rejection→keeps advancing) plus mocked unit coverage. Full detail in `docs/DEVLOG.md`'s "cont. 7" entry.
- Found and fixed real bugs via manual testing (not caught by unit tests alone — worth re-running the terminal chat after any change to `app/graph.py`): (1) `state.consult_count` (guard R-1) never reset between turns, silently capping every conversation at 3 total messages instead of capping consultations *within* a turn; (2) keyword-based routing sending weekday-only scheduling replies to the Info Advisor; (3) `_synthesize`'s precedence checked "info consulted" before the non-conclusive "sched consulted" branch, so an unrelated Info decline could steamroll a contextually relevant Sched verdict right after a slot offer; (4) `resolve_relative_date`'s silent "tomorrow" fallback on unrecognized expressions. All fixed and regression-tested.
- **2026-07-19 scheduling-message bugs** (found via manual REPL testing, all fixed + regression-tested, see DEVLOG for full detail): (a) the schedule offer/re-ask messages were hardcoded placeholders that never rendered the actual DB-verified slots — candidates were never told real dates/times; (b) a 2nd consecutive `continue` reply in an open-offer thread lost the offer context (only the single immediately-preceding turn's action was checked) — fixed via `_in_schedule_phase`/`_pending_offered_slots` in `app/graph.py`, which scan back to the last `schedule`/`end` action; (c) the Sched Advisor's confirmation prompt had no branch for "a different concrete date" (misclassified as decline) — added one; (d) `date_resolver` had no numeric-date support (`dd/mm/yy`, day-first) — added; (e) `date_resolver`'s literal `"tomorrow" in lowered` check missed typos like "tomororw" — added a narrow fuzzy-match fallback (difflib, cutoff 0.82) for "tomorrow" only (deliberately not extended to weekday names — Tuesday/Thursday score too close at that cutoff to safely fuzz).
  - **Known follow-up, explicitly deferred**: (c)-(e) above are all symptoms of the same root cause — `date_resolver.resolve_relative_date` is pure regex/dateutil pattern-matching, not the LLM-assisted-parse-then-dateutil-verify design spec §7.3 actually calls for (a prior session deliberately chose pure-`dateutil` for determinism/testability). Decided (2026-07-19) to keep patching narrowly for now rather than doing the full LLM-assisted rewrite as part of this session — revisit later.
